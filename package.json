{
  "name": "incant",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "NAN 2026 사전과제 — 자유 텍스트 주문 로그라이크",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "test:spell": "esbuild scripts/spell-regression.ts --bundle --platform=node --format=esm --outfile=node_modules/.cache/incant-spell-regression.mjs && node node_modules/.cache/incant-spell-regression.mjs",
    "preview": "vite preview"
  },
  "dependencies": {
    "phaser": "^3.90.0"
  },
  "devDependencies": {
    "typescript": "^5.9.2",
    "vite": "^7.1.0"
  }
}
