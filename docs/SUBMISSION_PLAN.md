# 제출물 계획 & 마일스톤

> NAN 2026 사전과제 마감: **2026-08-10** / 내부 목표 제출일: **2026-08-09 (1일 버퍼)**
> ⚠️ 5개 항목 중 하나라도 누락 시 심사 제외

## 1. 제출물 5종 매핑

| # | 제출물 | 형태 | 우리의 산출물 | 오너 |
|---|---|---|---|---|
| ① | 플레이 가능한 빌드 + 소스 | 웹(브라우저) + 전체 소스, GitHub Pages 링크 | 이 리포(public) + Pages 데모 URL | 전원 (R3 파이프라인) |
| ② | 플레이 동영상 | 30~60초, YouTube 링크 | 스토리보드 기반 촬영·편집 (§3) | R3 |
| ③ | 게임 소개·설명 문서 | PDF (개요·플레이 방법·실행 방법) | GDD 요약 + 스크린샷 + 실행 가이드 | R3 |
| ④ | AI 활용 기술 문서 | PDF (AI 도구·프롬프트·활용 내역) | AI_USAGE_LOG 기반 2부 구성 (§4) | R2 |
| ⑤ | 팀원 역할 기술서 | PDF | ROLES.md 기반 | R3 |

## 2. 주차별 마일스톤

| 주차 | 기간 | 목표 | 완료 기준 (DoD) |
|---|---|---|---|
| **W1** | 7/14~7/20 | 셋업 + 기술검증 | 리포·CI·Pages 동작. **입력→판정→파티클 1사이클** 데모 가능. 프록시 배포(팀 계정). GDD 팀 확정 |
| **W2** | 7/21~7/27 | 코어 루프 | 웨이브 전투(적 3종+), 마나/HP/보상, 방 진행. 파츠 라이브러리 v1 (8원소×6폼). GeminiJudge 실연동 |
| **W3** | 7/28~8/3 | 보스 + 폴리싱 | 기억하는 보스(내성·카운터·대사), 3스테이지, 파츠 12폼 완성, 사운드·연출, 밸런스 1차 |
| **W4** | 8/4~8/10 | QA + 제출물 | 아래 상세 일정 |

> **W1 기술 DoD 완료 (2026-07-15)**: PR #3·#4·#7·#8 취합, Pages 배포 및 라이브 `[gemini]` 판정까지 검증했다. 게임명 투표는 7/20까지 별도 진행한다.

### W4 상세

| 날짜 | 작업 |
|---|---|
| 8/4~8/6 | 전체 플레이테스트(외부 지인 포함), 버그 수정, 96조합 전수 QA |
| **8/7** | **콘텐츠 프리즈** (밸런스 수치만 조정 가능) |
| **8/8** | **코드 프리즈** (치명 버그만 수정). 영상 촬영 |
| 8/9 | 영상 편집·업로드, PDF 3종 최종화, **제출** |
| 8/10 | 예비일 (제출 확인·수정) |

## 3. 영상 스토리보드 (30~60초)

| 구간 | 시간 | 장면 |
|---|---|---|
| 콜드 오픈 | 0~8s | 전투 중 Enter → 슬로모션 → "얼음 감옥" 타이핑 → 적이 얼음에 갇힘. 주문명 각인 |
| 문제 제기 | 8~15s | 자막: "정해진 스킬은 없다. 당신의 문장이 주문이 된다" |
| 창의 주문 몽타주 | 15~35s | 서로 다른 3~4개 주문 연속 발동 (원소·폼이 확연히 다른 조합으로 선정) |
| ── 몽타주 후보 갱신(7/22) | | ① "철벽 무적"(피해 0 흡수) ② "번개 돌진"(순간이동) ③ "벌레 군체"(3기 소환) ④ **"분신을 만들어 지그재그로 돌진시켜라"** — L3(#101) 게이트 통과 시 **최우선 채택**: "LLM이 행동을 설계한다"가 한 컷에 보인다. COMBO +30% 자막 노출로 다양성 시스템도 함께 |
| 기억하는 보스 | 35~50s | 보스: "지난번엔 화염이었지" → 화염 실드 → 플레이어가 새 주문으로 파훼 |
| 클로징 | 50~60s | 로고 + "Built with AI, Powered by AI" + Pages URL |

- 녹화: OBS 1080p60 / 편집: 팀 보유 툴 (자막 필수 — 무음 시청 대비)
- 업로드: YouTube 공개(또는 미등록) — 마감 전 링크 접근 테스트

## 4. AI 활용 기술 문서 (제출물 ④) 구성안

**1부 — 게임 속 AI (제품)**
- 주문 판정 아키텍처: 프롬프트 전문, JSON 스키마, 검증·폴백 체인, 캐싱
- 밸런스 가드레일: LLM을 신뢰하지 않는 설계 (클램프·반복 패널티·쿨다운)
- 기억하는 보스: 히스토리 분석 → 내성/카운터, 런 간 기억, 대사 생성
- 인프라: Cloudflare Worker 프록시, 레이트리밋, (옵션) WebLLM 이중화

**2부 — AI로 만든 게임 (프로세스)**
- 3인 × 에이전트(Claude/Codex) 협업 워크플로
- 실제 프롬프트·산출물 사례: 파츠 config 대량 생산, QA 스크립트 생성 등
- AI_USAGE_LOG 통계 (도구별·작업별 활용 비중)
- 배운 점: 에이전트 지휘 노하우 (본선 '에이전트 설계서'와 직결되는 어필 포인트)

## 5. 리스크 & 대응

| 리스크 | 대응 |
|---|---|
| Gemini 무료 티어 한도/장애 | 캐시 + MockJudge 폴백 내장. 심사 기간 정책은 **§6** 참고 |
| 심사위원 환경(브라우저) 이슈 | 크롬·엣지 테스트 필수. WebGL 미지원 시 안내 문구. (옵션) WebLLM은 부가 모드로만 |
| 판정 품질 불만족 | W2에 GeminiJudge 실연동 후 즉시 튜닝 시작 (프롬프트 개선 기간 3주 확보) |
| 일정 지연 | GDD §10 스코프 가드 순서대로 컷. 코어(주문 전투+기억 보스)만은 사수 |
| 제출 누락 | 8/9 제출 후 팀원 3인 교차 확인 (5종 링크 전부 시크릿 창에서 열어보기) |

## 6. 심사 기간 Gemini 할당량 정책 (초안 — R2 임재윤, 2026-07-19)

> 최악 케이스: **심사위원이 몰려 무료 티어 일일 한도(RPD)를 소진** → 판정이 429로 실패. 게임은 폴백으로 계속 돌지만 판정 품질이 저하된다.

### 6-1. 현재 사실 (실측·공식 문서 기준)
- 모델: **`gemini-3.5-flash-lite` 명시 핀** (2026-07-23~, 코덱스 #107). ⚠️ 기존 `-latest` 별칭이 **2.5→3.1→3.5로 자동 드리프트**했고 `gemini-2.5-flash-lite`는 **폐기됨(404 NOT_FOUND)** → **`-latest` 금지, 명시 핀 필수**. (Node/정상 UTF-8 실측으로 3.5 판정 품질·behavior 정상 확인 — #110. 참고: Windows curl 한글 인코딩 오진 주의.)
- 무료 티어 한도(2.5 Flash-Lite): **15 RPM · 250K TPM · 1,000 RPD**. (지시서의 "RPD 500"은 보수치 — 실제 1,000)
- 유료(PayGo) 가격: 입력 **$0.10 / 1M tokens**, 출력 **$0.40 / 1M tokens** (Batch/Flex 50%↓).

### 6-2. 데모 호출 프로필 (캐시 반영)
| 호출 | 빈도 | 캐시 |
|---|---|---|
| 판정 `/` | 신규 문장당 1회 (동일 문장 반복은 0회) | localStorage, `incant:judge:v2:meaning-v2.1:` |
| 보스 대사 `/boss-line` | 보스전당 1회 | 런 요약 키 |
| 진화·융합 `/evolve-name` | 런당 1~2회 | localStorage, `incant:evolvename:name-v1.0:` |

- **빈 캐시 1회 완주 ≈ 18~28 콜** (대부분 판정, 다수는 반복·캐시로 절감).
- 무료 RPD 1,000 ≈ **일 35~55회 완주** 여력. 다수 동시 접속 시 15 RPM이 먼저 병목.
- 토큰당 비용 극소: 판정 1콜 ≈ 820 tokens ≈ **$0.00014**. **1만 콜 ≈ $1.4**.

> **⚠️ C안(능동형 마나) 채택 영향 (#53-5, 2026-07-21 확인)**: 시전 경제 C안이 **글로벌 쿨다운 3초 하한을 0.4초 인풋락으로 교체**(`startGlobalCooldown` 미사용, `castInputLockSeconds:0.4`). → 마나 넉넉할 때 **버스트 시전율이 Gemini 무료 15 RPM을 초과** 가능 → 그 구간 429 → Mock 폴백(무중단이나 판정 품질 저하). **완충**: 캐시(반복 무료)·마나 게이팅(지속율 제한)·폴백. **결론**: 평상시 문제없음, 마나 버스트 시 일부 Mock. → **§6-4의 심사기간 유료 전환 권고가 더 중요해짐**(유료 RPM↑). 부수: 프록시 `RATE_LIMIT_PER_MIN=20`이 Gemini 15보다 느슨 → 15로 맞추면 신호 명확(사소).

> **⚠️ 마나 경제 진화 재점검 (#121·#122, 2026-07-23)**: C안 위에 **영창 환류**(#121: 수동 주문으로 킬 → 마나 +6)와 **감쇠 시전**(#122: 마나<비용이면 거부 대신 남은 마나 비례로 위력 축소 시전, 마나≥5까지 항상 시전)이 추가됨. → **둘 다 시전 빈도를 올리지만 기전이 다르다.** 환류는 마나 회복으로 지속 시전을 쉽게 해 **직접** 호출을 늘린다(영창→킬→영창). 감쇠는 **호출을 늘리지 않는다** — 비용(`spell.cost`)이 판정 결과에 들어 있어 마나 검사는 판정 **이후**에만 가능하고(`ProtoScene.castFromText`: 저지 호출 → 마나 검사 → 거부), #122 이전에도 마나 부족 시 호출은 이미 나가고 **버려지던** 것이었다. 즉 #122는 버려지던 호출을 결과 있는 시전으로 바꾼 것(**호출당 효율 개선**)이고, 빈도 상승은 **행동 변화**로 온다 — 저마나 영창이 더 이상 헛수고가 아니라 그 구간에도 영창하게 됨. + 다양성 보너스(#94)가 변주를 유도 → 신규(캐시 미스) 문장↑. **순효과: Gemini 호출 빈도가 옛 경제 대비 상향.** 단 한 플레이어는 캐시·시전 간격(초 단위)으로 여전히 15 RPM 아래일 공산 큼. **진짜 리스크 = 심사위원 동시 접속**(15 RPM은 프로젝트/키 단위 한도) → 그 구간 429→Mock 폴백(무중단, 품질 저하). **완충 강화됨**: `RATE_LIMIT_PER_MIN`을 코덱스 #107이 20→**15**로 Gemini 정합. **결론: §6-4 심사기간 유료 전환 권고가 더욱 강해짐**(유료는 RPM 상한 제거, 비용은 여전히 무시 가능 — 1만 콜 ≈ $1.4).

### 6-3. 대응 (노력 순 — 위에서부터)
1. **이미 내장 (코드 0 변경)**: localStorage 캐시 + 2.5초 타임아웃 폴백 + MockJudge. **429여도 게임 무중단**(판정 품질만 저하). ← 최소 안전망 확보됨.
2. **캐시 프리워밍**: 영상·시연용 대표 코퍼스를 사전 판정해 캐시 시드 → 흔한 입력은 호출 0. (심사 직전 `snapshot:quality` 계열로 워밍)
3. **결제 활성화 (권장)**: 무료→유료 전환 시 RPD 한도 대폭 상승, **비용은 사실상 무시 가능**(만 콜 $1.4). 키·코드 불변, 대시보드 토글만.
4. **키 로테이션 (무료 유지 시)**: 무료 키 2~3개를 프록시가 429 시 순환 → 무료로 RPD 2,000~3,000. `worker.js` 소폭 수정 필요.

### 6-4. 권고
- **심사 기간엔 결제 활성화(유료 전환)가 가장 안전·저렴** — 최악 트래픽도 몇 달러. 그 전까진 내장 폴백으로 충분.
- **재현성**: 심사 창 동안 모델을 `gemini-2.5-flash-lite` 명시 핀으로 고정 검토 — `-latest`가 심사 중 자동 갱신되어 판정 거동이 바뀌는 것을 방지 (안정성 vs 자동 승계 트레이드오프).

_(출처: Gemini API Rate limits / Pricing 공식 문서, 2026-07 조회.)_
