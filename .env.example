# 판정기 설정 — 둘 다 선택사항. 아무것도 안 해도 기본 팀 공용 프록시로 실제 Gemini 판정이 동작한다.
#
# (선택) 다른 프록시(예: 유료 키 배포본)를 쓰려면 URL 지정:
# VITE_JUDGE_PROXY_URL=https://incant-judge-proxy.<your-subdomain>.workers.dev
#
# (선택) 실제 Gemini 대신 MockJudge(키워드, 오프라인·할당량 절약)를 강제하려면:
# VITE_JUDGE_MOCK=1
