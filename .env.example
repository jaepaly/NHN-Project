# 주문 판정 프록시 URL — 이 값이 있으면 실제 Gemini 판정(GeminiJudge), 없으면 MockJudge.
# 배포 방법은 proxy/README.md 참조. 실제 값은 .env 에 넣는다 (.env 는 커밋 금지).
VITE_JUDGE_PROXY_URL=https://incant-judge-proxy.<your-subdomain>.workers.dev
