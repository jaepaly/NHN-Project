# R1 세션 인수인계서 — 2026-07-31

이 문서는 다음 세션이 별도 대화 맥락 없이도 현재 프로젝트 상태, 이미 확정된 설계, 진행 중인 PR/이슈, 남은 결정과 작업을 이어갈 수 있도록 작성한 인수인계서다.

## A. 새 세션 시작 직후의 실행 장부 — 생략 금지

이 표는 서술형 요약을 읽지 않아도 현재 일을 놓치지 않게 하는 우선순위 장부다. 새 세션은 이 표를 먼저 읽고, 각 항목의 “다음 행동”이 끝나거나 명시적 대기 상태가 될 때까지 다른 일을 새로 시작하지 않는다.

| 우선 | 항목 | 상태 | 새 세션의 다음 행동 | 차단 조건 / 금지 |
|---|---|---|---|---|
| P0 | **PR #312** | Open, ready, clean. 현재 브랜치의 본 작업 | 새 리뷰·코멘트·CI를 확인하고, 승인 시에만 머지 진행. 머지 후 main SHA를 기록 | #312에 #304 terrain, #244 밸런스, #307 대각선 기능을 끼워 넣지 말 것 |
| P0-후속 | **PR #307 대각선 경로** | **미완료. 잊으면 안 됨.** owner의 `CHANGES_REQUESTED`가 유효 | **#312 머지 직후** 최신 main에 리베이스 → 새 가변 경로 계약에서 기능 필요성 재판단 → 유지면 규칙 재설계 및 2,000 seed 성능 재측정 → owner review 해결 | #312 전에는 코드를 고치거나 머지하지 말 것. 내가 남긴 리뷰는 Dismiss됐을 뿐 owner review는 취소되지 않음. “대각선 기능 폐기” 결정은 아직 없음 |
| P1 | **#265 돌진/장벽** | R1 담당, 설계 논의만 진행됨 | 위 §7의 guided-motion 설계를 이슈에 제안하고 사용자/팀 승인 후 별도 브랜치·PR 구현 | 원문에 `돌진` 단어가 있을 때만 발동하도록 LLM 해석을 잘라내지 말 것. 장벽 관통 상태를 방치하지 말 것 |
| P1 | **PR #313 / #311 placement** | PR 구현·1차 라이브 검증 완료, 머지 대기 | 새 코멘트/CI 확인. 머지 후 502가 재현되면 응답 body·`cf-placement`·p50/p95/error rate 관측 | placement가 강제 보장은 아님. 프롬프트·모델·키·스키마 변경으로 원인을 흐리지 말 것 |
| P1 | **#267 적 강화** | R3의 추가 답변 대기 | R3에게 시연 씬의 상정 루프/진행 구간, 루프 강화에 공격력 포함 여부를 받으면 평균 성장/적 강화 기준 산정 | 실제 플레이어 성장치에 비례한 즉시 적 강화 부활 금지. 경로별 위험–보상 차이 평준화 금지 |
| P2 | **#264 / #272 / #309** | 맵 배선 이후 통합 검증 항목 | #312 머지 후 실제 그래프 기반 표시/도착/keep-out 회귀를 점검 | 8칸·6방 같은 고정 상수로 표시하지 말 것 |
| P2 | **#258 / #244 전투 프리셋** | 설계 완료, 실전 수치 검증 전 | 실제 프리셋 적용 후 클리어 시간과 영창 사용을 플레이테스트로 계측 | 생성기 경로 수를 먼저 줄여 런타임을 맞추는 결론으로 가지 말 것 |
| P2 | **영창 판정기** | 후보58/PR #303 계열 품질 검증 후, 안정성·실플레이 추적 단계 | 실제 플레이 품질과 fallback/지연을 관측. 새 튜닝은 30종 과적합을 먼저 경계 | `docs/SEQUENCE_*`를 일괄 삭제·커밋하지 말 것 |

### #307을 위한 독립 실행 체크리스트

#307은 단순 “리뷰 대기”가 아니다. 아래 순서를 **반드시** 수행한다.

1. #312의 실제 merge commit을 확인한다.
2. #307 branch를 그 최신 main에 리베이스한다. 충돌 시 새 `mapGenerator.ts`의 #240 규칙을 우선한다.
3. 대각선 경로가 lane형 계층 DAG의 가독성·선택 다양성에 실제로 필요한지 먼저 재판단한다. 필요하지 않다면 폐기/닫기 제안도 가능하다.
4. 유지한다면 “전 경로 8방”, “스테이지별 2~4개 고정 배치”, “기존 8방 분포”라는 전제를 모두 제거하고, 10/11/12방 가변 경로에서의 배치 규칙을 새로 정의한다.
5. owner review에서 지적한 탐색 비용을 새 구현에서 측정한다. 기존 측정은 2,000 seed 평균 131.79ms, 최악 2,259ms였으며, 160회 바깥 재시도 × 내부 최대 128회 탐색이 원인이었다.
6. 단순한 목표는 1 seed 최악 50ms 이하이며, 내부 대각선 layout 재시도를 2~4회 수준으로 낮추고 하향 스윕을 제거하는 방안이 제안돼 있다. 단, 새 생성기에서 성능·분포를 함께 측정한 결과로만 채택한다.
7. `test:mapgen`만 통과했다고 끝내지 않는다. 시간 상한을 포함한 별도 회귀를 추가하거나 재현 가능한 성능 측정 결과를 PR에 남긴다.
8. 위가 끝난 뒤에만 owner의 `CHANGES_REQUESTED` 해소를 요청한다.


## 0. 가장 먼저 읽을 내용

- 작업 경로: `C:\Users\melsh\OneDrive\문서\GitHub\NHN-Project`
- 현재 브랜치: `codex/repair-map-generator-288-main`
- 현재 HEAD: `7793da4 fix(run): restore #240 map generator invariants`
- 현재 브랜치는 `origin/main`의 `6754423` 위에 있으며, PR #312의 head다.
- 현재 worktree에는 코드 변경은 없고, 영창 판정 실험 산출물인 `docs/SEQUENCE_*` 다수 파일이 untracked 상태로 남아 있다. 이 파일들은 사용자 실험 결과이며 임의 삭제·정리·커밋하지 않는다.
- **Gemini Worker·배포·키·fallback·placement 관련 작업을 시작할 때는 반드시 `WORKER_DEPLOYMENT_HANDOFF_2026-07-31.md`를 먼저 읽는다.** 이 문서는 별도 운영 절차와 실제 장애 이력을 담으며, 여기의 요약보다 우선한다.
- **#307은 현재 진행 항목에서 제외된 것이 아니다.** #312가 먼저 머지되기 전까지는 대기하고, 머지 후 #307을 새 생성기 기준으로 리베이스한 다음 대각선 경로를 유지할지와 성능을 다시 판단해야 한다. 기존 owner 리뷰의 성능 `CHANGES_REQUESTED`는 여전히 유효하다.
- R1의 유의미한 코드/설계/밸런스/플레이테스트 작업을 완료하거나 커밋·PR로 올릴 때는 `.agents/skills/r1-work-log/SKILL.md`를 읽고 `docs/AI_USAGE_LOG.md`, `docs/R1_PROJECT_WORK_LOG.md`를 함께 갱신한다.
- 제출 전 `git diff --check`와 두 로그의 diff를 확인한다.

## 1. 현재 열린 PR과 이슈

### PR

| 번호 | 상태 | 요지 | 다음 세션의 판단 |
|---|---|---|---|
| [#313](https://github.com/jaepaly/NHN-Project/pull/313) | Open, clean | Gemini Worker placement를 `gcp:asia-northeast1`로 지정 | R2 배포 정합성 확인 후 머지 대기. 1차 라이브 검증은 성공했음 |
| [#312](https://github.com/jaepaly/NHN-Project/pull/312) | Open, clean, ready | #288 생성기를 #240 설계 계약에 맞게 복구 | 현재 R1의 핵심 진행 PR. 리뷰·머지 대기 |
| [#309](https://github.com/jaepaly/NHN-Project/pull/309) | Open, clean | 다음 방 지도 라벨/hover 보정 | 별도 검토 필요. #312와 직접 충돌한다고 단정하지 말 것 |
| [#307](https://github.com/jaepaly/NHN-Project/pull/307) | Open, clean, owner review `CHANGES_REQUESTED` | 일반 생성 맵 대각선 경로 | 제가 남긴 변경 요청 리뷰는 GitHub 제약으로 삭제할 수 없어 Dismiss 처리했음. 기존 소유자 리뷰는 여전히 남아 있음. #312 머지 후 새 생성기 기준으로 재검토하는 것이 안전 |
| [#261](https://github.com/jaepaly/NHN-Project/pull/261) | Open, dirty | R2 로그 보강 | R1 코드 작업 아님 |
| [#244](https://github.com/jaepaly/NHN-Project/pull/244) | Open, dirty | 방 전투 프리셋 개편 설계 | R1 설계 문서. 일반/함정/엘리트 전투 목표시간과 웨이브 프리셋 기준을 담음 |
| [#240](https://github.com/jaepaly/NHN-Project/pull/240) | Open, dirty | 분할 맵 생성 설계 | #214의 설계 기준. #312가 이를 코드 계약으로 복구하는 관계 |

### 핵심 열린 이슈

- [#311](https://github.com/jaepaly/NHN-Project/issues/311): Gemini 지역 제한으로 데모 fallback. PR #313이 해결안을 구현했다.
- [#272](https://github.com/jaepaly/NHN-Project/issues/272): 맵 그래프 경로/미니맵 라벨 상수 불일치. 8칸/6방 같은 값을 맵 데이터 상수로 고정하면 안 되며, 실제 생성 그래프에서 방문 순번을 표시하는 방향이 현재 합의다.
- [#267](https://github.com/jaepaly/NHN-Project/issues/267): 적 성장 스케일링과 전투 프리셋 역할 분리. R3의 시연 루프/진행 구간과 공격력 강화 여부 답변 대기.
- [#265](https://github.com/jaepaly/NHN-Project/issues/265): 요청하지 않은 강제 돌진과 구조물 상호작용. R1에 배정되어 있으며 현재 설계 승인·구현 전.
- [#264](https://github.com/jaepaly/NHN-Project/issues/264): 프리셋 지형의 도착 지점 keep-out. #246 잔여 사항.
- [#258](https://github.com/jaepaly/NHN-Project/issues/258): 룸 전투 목표시간/영창 횟수. 현재 방향은 일반 47초, 함정 54초, 엘리트 60초이나 추가 밸런스 검증이 필요하다.
- [#214](https://github.com/jaepaly/NHN-Project/issues/214): 분기형 맵 그래프 전체 기획의 상위 이슈.
- [#203](https://github.com/jaepaly/NHN-Project/issues/203), [#200](https://github.com/jaepaly/NHN-Project/issues/200): 추상 영창의 시퀀스 판정과 표현 품질.

## 2. 최근 머지된 기반과 절대 혼동하면 안 되는 것

현재 `origin/main`에는 #310까지 머지되어 있다.

- #304/#310은 #298의 잘못된 용어·가중치·프리셋 변경을 시정했다.
- `trapProfile.hazard`는 함정방의 위험지대 프리셋(안전 회랑 포함)이고, `MapNode.terrain`의 용암/독지대는 방 종류와 독립적인 바닥 지형 위험요소다.
- 용암/독지대는 함정방 여부와 무관하게 등장할 수 있는 별도 위험요소다. 이를 함정방 전용으로 해석하면 안 된다.
- `MapNode.terrain`은 실제 런에 배선되어 있다.
- 정적 구조물 장벽도 실제 전투방에 들어왔다. 돌진이 장벽을 관통하는 문제는 아직 해결되지 않았다.

## 3. PR #312 — #288 생성기 복구의 실제 내용

### 목적

#288이 MapGraph 연결과 `MapNode` 계약을 통합했지만, 생성기 내부를 기존 프로토타입과 다르게 재작성하면서 #240 설계와 어긋난 고정 8방·스테이지 강제·실제 보상값 기반 비교가 들어갔다. #312는 #288의 통합 골격은 유지하고 생성기 규칙만 #240으로 복구한다.

### 구현된 규칙

`src/run/mapGenerator.ts`:

- seed 기반 결정성, MapGraph 최종 계약, 포탈/실제 encounter 연결, preset fallback은 유지.
- 경로 길이를 전역 8방으로 고정하지 않는다.
- 스테이지 경로 목표는 `[[3,4],[4,5]]`이며, 실제 전체 경로는 seed에 따라 10/11/12방으로 분산된다.
- 설계용 위험/보상 축만 사용한다.
  - C: risk 1 / reward 0
  - E/T: risk 2 / reward 0
  - `$`: risk 0 / reward 1
  - A: risk 1 / reward 1
  - boss: 0 / 0
- 실제 플레이어 HP나 실제 보상 수치로 경로를 지배하지 않는다.
- 유효 DAG, 모든 경로 보스 도달, 분기/방 수 제한, 스테이지 보상 최소, 경로별 domination 회피 등을 candidate layout 선택에서 검증한다.
- 스테이지 2 첫 child는 combat으로 고정한다.
- 후보 레이아웃은 특수방 존재, 추가 combat, 비전투 비율 근접 등을 기준으로 선택한다.
- 스테이지 내부 분기 종류를 억지로 모두 강제하지 않는다.

### 검증 완료

- `npx.cmd tsc --noEmit`
- `npm.cmd run test:mapgen` — 500 seed, 생성·MapGraph 계약·경로 중복 방지·보상 최소·위험/보상 축·웨이브/함정 계약·결정성 통과. 전체 경로 10/11/12 분포 확인.
- `npm.cmd run test:mapgraph`
- `npm.cmd run test:mapwiring`
- `npm.cmd run build`
- `git diff --check`

### PR 설명에 명시한 범위

- #288의 통합 기반은 유지한다.
- #240에 없는 고정 8방, 스테이지 branch 강제, 실제 보상 규모 비교는 제거한다.
- #304의 terrain wiring/위험지대 롤백이나 #244 전투 밸런스는 #312 범위가 아니다.

### 다음 세션에서 할 일

1. PR #312 리뷰/코멘트 확인.
2. 승인되면 머지 후 `origin/main` 기준으로 후속 PR들의 충돌 여부 확인.
3. **PR #307 후속 처리:** 지금은 코드를 고치거나 머지하지 않는다. #312 머지 직후 #307을 최신 `main`에 리베이스하고, 새 가변 경로 길이 생성기에서 대각선 경로가 여전히 필요한지 먼저 결정한다. 유지한다면 배치 규칙을 새 계약에 맞춰 다시 작성하고 2,000 seed 성능 측정을 재실행한다. 기존 owner 리뷰가 지적한 수치는 평균 131.79ms/최악 2,259ms이며, 내부 32회 탐색과 8방 전제가 핵심 쟁점이다. 대각선 기능 자체를 무조건 폐기한다는 결정은 아직 없다.

## 4. 맵·방 구조 설계 합의

- 맵은 격자형 방 복귀 구조보다 lane형 계층 DAG로 본다.
- 각 방의 다음 출구 포탈은 오른쪽, 도착 포탈은 왼쪽에 배치하는 방향이 현재 가장 일관적이다. 포탈 이동은 마법 지대이므로 물리적 좌표의 되돌아감과 실제 그래프 복귀를 동일시하지 않는다.
- 포탈로 도착한 뒤 방 내부 시작 위치는 중앙 고정이 기본 방향이다. 아래쪽 고정 스폰은 포탈 방향성과 위험지대 중앙 안전영역 문제를 키울 수 있다.
- “방 전체 구조”는 맵 그래프가 아니라 방 하나 내부의 스폰·입구·출구·전투 배치·기믹·웨이브 구성을 의미한다.
- 일반/함정/엘리트방은 프리셋으로 구성하고, 한 런 내부 진행도에 따라 난이도 티어를 올린다. 티어는 런 간 플레이어 성장에 대응하는 값이 아니다.
- 동일 프리셋을 방 종류가 공유할 수 있지만, 엘리트 적용 시 실드/생존력/웨이브 변형 같은 별도 적용 방식이 필요하다.
- 일반방은 2~3 웨이브 프리셋을 기본으로 하고 1웨이브는 지양한다. 함정방도 일반방 적 프리셋을 공유할 수 있으나 기믹 대응 때문에 일반과 동일한 시간으로 보지 않는다.
- 현재 목표시간 가설: 일반 47초(중앙 영창 3회 목표로 변경), 함정 54초, 엘리트 60초, 스테이지 보스 78초, 최종 보스 90초, 보물/제단 20초.
- 이 값은 목표 상한이 아니라 평균적인 다영창 플레이의 설계 기준이다. 플레이어가 영창을 거의 쓰지 않거나 광역기로 빠르게 끝내는 플레이는 별도 결과로 본다.
- 런 평균 목표는 약 10분. 과거 5,000 seed 검증 가설은 평균 10분 16초, 중앙값 10분 09초, 최단 8분 35초, 최장 11분 45초, 8~12분 전부 충족이었다. 보상 선택·포탈 선택·전환 연출·입력/판정 대기 등 오버헤드를 포함해야 한다.
- 플레이어 실제 성장에 따라 적 스탯을 즉시 같은 비율로 올리는 방식은 위험–보상 선택을 무효화하므로 부적절하다. 적 강화는 기본적으로 루프 단계 고정 강화와 한 런 내부 프리셋 티어를 분리한다.

## 5. #267 적 성장/프리셋 역할 분리

R3는 다음 방향에 동의했다.

- 실제 플레이어 성장치 연동 enemy scaling 제거.
- 루프 단계에 따른 기본 강화는 유지.
- 맵 내부 경로별 성장 기회 차이는 위험–보상 설계이므로 유지.
- 서로 다른 seed의 맵 전체 평균 성장 기회 차이를 보정할지는 #240 머지 후 실제 분포를 측정한 뒤 판단.
- `playerPowerIndex`는 적 스탯 계산이 아니라 밸런스 측정 로그용으로만 존치.

아직 답이 필요한 항목:

1. 시연 씬이 몇 번째 런의 어느 진행 구간을 상정하는가.
2. 루프 기반 적 강화에 체력뿐 아니라 공격력도 함께 적용할 것인가.

시연 로드아웃은 프리셋 티어를 임의로 후반 티어로 덮지 않는다. 상정 루프의 평균 성장값에 맞는 루프 강화와, 런 내부 진행도에 맞는 프리셋 티어를 독립적으로 적용해야 한다.

## 6. 지형·함정방·keep-out 결정

- 위험지대 원형 장판은 고정 위치가 아니라 프리셋/안전영역 계약에 맞춰 생성되어야 한다. 십자 안전지대와 원형 위험지대가 겹쳐 중앙 접근이 불가능하거나 안전지대가 분리되지 않는지 회귀 검사가 필요하다.
- 위험지대와 침묵방에서는 십자 안전지대 위 몬스터 생성 금지를 적용하는 방향이다.
- 용암/독지대와 정적 장벽은 중앙 안전지대·십자 안전지대를 침범하지 않게 하는 별도 keep-out 규칙이 필요하다.
- 출구 포탈이 활성화될 때 지형을 제거할지 여부는 아직 선택지다.
  - 제거한다면 출구 포탈 keep-out은 필수 아님.
  - 제거하지 않는다면 출구 포탈 위치에도 keep-out을 적용해야 함.
- 자연스러움만 보면 지형을 그대로 두는 쪽이 좋지만, 구현 계약을 먼저 명시하고 선택해야 한다.
- #265의 돌진은 현재 장벽을 관통한다. 지형을 단순히 통과시키거나, 원문에 “돌진”이 있을 때만 허용하는 식으로 성급하게 고치지 않는다.

## 7. #265 현재 핵심 문제와 제안 방향

현재 코드:

- `src/scenes/ProtoScene.ts`의 `castSelfBuff`/`performDash`
- `src/combat-core/player/selfBuffConfig.ts`의 `resolveSelfBuff`
- `performDash`는 `lastMoveDir` 또는 가장 가까운 적 방향을 사용하고, `worldBounds`만 적용한다.
- 실제 사각 장벽과의 sweep collision이 없어 장벽을 관통한다.
- 입력 중 정지 상태에서 `lastMoveDir`이 0이면 적 방향으로 돌진할 수 있다.

현재 논의된 구현 방향:

- LLM이 해석한 이동 의도를 유지한다. 원문에 “돌진”이라는 정규식이 반드시 있어야만 발동하게 만드는 것은 병목이 될 수 있으므로 아직 확정하지 않았다.
- 장벽은 관통하지 않도록 한다.
- 이동 경로를 충돌 지점까지 sweep하고, 남은 이동량의 법선 성분을 제거해 벽면을 따라 한 번 슬라이드한다.
- 동일 시퀀스에서 같은 장벽에 같은 방향으로 재충돌하면 해당 이동 단계를 종료한다.
- 이동 1회당 슬라이드는 최대 1회. 남은 거리가 작으면 미세 떨림 없이 종료한다.
- 공격/대기/큰 방향 전환 이후에는 충돌 기억을 초기화한다.
- 최종 실제 도착 좌표 기준으로 잔상·이펙트를 만든다.
- A* 같은 완전 경로 탐색은 필요 없다.

이 방향은 아직 승인 전이다. 사용자가 “이 정도면 된다”고 한 것은 현재 제안의 위험 검토가 충분하다는 의미이지, 코드 구현 승인까지 완료했다는 뜻으로 기록하지 않는다. 다음 단계는 #265에 설계 코멘트로 위 방향을 제안하고, 승인 후 별도 R1 구현 PR을 만드는 것이다.

## 8. 영창 판정기 전체 진행과 현재 기준

### 이미 진행된 방향

- 초기 30종 테스트케이스와 expected result를 기준으로 worker 판정 결과를 비교했다.
- 초기 30종은 AI 생성 성격도 있어 최종 계약 그 자체가 아니다. 목표는 케이스 과적합이 아니라 시각적으로 만족스럽고 유저 상상에 가까운 영창이다.
- 단일 영창과 시퀀스 영창을 엄격히 분리하기보다, 모든 입력을 시퀀스 하위 범주로 보고 복합적 표현을 적극 확장하는 방향으로 여러 차례 수정했다.
- 이동만 하는 move-only 주문은 게임에서 효용이 낮고 밋밋할 수 있으므로, 이동 강조 입력은 공격/제어/원소 효과와 결합되는 것을 선호한다. 다만 이동 강조가 아닌 입력까지 무조건 이동/복합으로 만들면 다양성이 줄어드므로 의미 기반 판단이 필요하다.
- 순수 이동을 임의 공격으로 바꾸는 규칙은 폐기 방향이다. 대신 `파이어 대시`, `카타리나 순보` 같은 이동 자체가 공격 의미를 갖는 입력은 원소/공격을 자연스럽게 결합할 수 있다.
- 원소 복수 사용은 “불사조”처럼 의미적으로 복수 원소가 자연스러운 경우 허용한다. 모든 입력에 여러 원소를 넣는 것은 금지.
- 연사/공격 입력은 단일로만 나오지 않고, 여러 공격 사이의 `wait`를 포함한 반복·분할 패턴도 허용한다. 단, sequence 마지막에 불필요한 `wait`만 남는 것은 로컬 후처리로 제거하는 방향을 적용했다.
- 이동 duration은 “찰나”, “빛보다 먼저” 같은 빠른 이동 표현에서 짧게 나와야 한다. 기본 sequence 평균 시간은 과도하게 길지 않으며, 최근에는 전반적인 이동 속도를 빠르게 하는 방향을 적용했다.
- 팔원소 대합창 같은 복합 입력은 응답시간/재시도 때문에 fallback이 발생할 수 있어 프롬프트 복잡도와 공통 timeout을 함께 관찰해야 한다.

### 코드/운영 기준

- 현재 worker는 all-plan 계열 프롬프트와 공통 6초 timeout 방향을 사용한다.
- 단순 입력만 짧은 timeout으로 별도 처리하면 fallback 리스크가 커질 수 있어 공통 timeout을 우선하는 방향이다.
- 실 플레이에서는 2~5초 응답이 느리게 느껴지지만 fallback보다는 안전하다. placement 적용 전 `불사조의 낙화`가 지역 제한으로 502/fallback을 냈고, placement 적용 후 5회 모두 HTTP 200이었다.
- `logs/play.jsonl`은 DEV 응답 시간 로그다. production Worker는 이 파일을 저장하지 않는다.
- local fixture 제거 실험이 진행되었으며, 현재 worktree의 `docs/SEQUENCE_*`는 그 실험·후보별 결과다. 캐시는 InPrivate 탭으로 우회하기로 했으므로 불필요하게 캐시 로직을 수정하지 않는다.
- 필살 영창은 추후 일반 worker prompt와 다른 prompt로 별도 LLM 호출을 붙일 수 있다. 현재 범위는 아니다.

## 9. PR #313 / Gemini placement

PR #313은 `proxy/wrangler.toml`에 다음만 추가한다.

```toml
[placement]
region = "gcp:asia-northeast1"
```

프롬프트, API key, 모델, schema, Worker 코드는 변경하지 않는다.

2026-07-31 1차 라이브 검증:

- `불사조의 낙화` 5회
- HTTP 200: 5/5
- Worker 502/Gemini 지역 오류: 0/5
- 2.86~3.95초
- 응답에 `cf-placement: remote-NRT` 확인
- 보정 재시도로 요청당 Gemini upstream 2회, 총 10회 외부 호출

Placement는 강제 리전 보장이 아니라 hint이므로, 머지 후 간헐 502가 재현되면 응답 본문과 `cf-placement`, p50/p95/error rate를 함께 기록한다.

## 10. 다음 세션의 권장 작업 순서

1. #312, #313의 새 리뷰/코멘트를 확인한다. #313은 placement 라이브 검증 결과와 저장소 정합을 확인한 뒤 머지 대기.
2. #265에 구조물 충돌 보정 설계를 제안한다. 사용자가 승인하기 전에는 코드 구현하지 않는다.
3. #312가 머지되면 #307의 대각선 경로 필요성을 새 생성기 기준으로 다시 평가한다. 기존 owner review의 성능 지적(2000 seed 평균 131.79ms, 최악 2259ms, 내부 32회 탐색)이 남아 있다는 점도 함께 확인한다. 단, 이 수치는 #312 이전 8방 기반 PR의 측정임을 분명히 한다.
4. #267은 R3가 시연 루프/구간과 공격력 강화 여부를 확정할 때까지 대기한다.
5. #264/#272/#309의 맵 표시·도착 keep-out은 실제 MapGraph가 main에 들어온 뒤 통합 테스트로 확인한다.
6. #258은 전투 밸런스 구현보다 실제 프리셋/플레이테스트에서 일반 47초·함정 54초·엘리트 60초와 평균 영창 횟수 3/4~5/5를 측정하는 단계다.
7. 영창 판정기는 추가 최적화보다 종합 코드 검증과 실제 플레이테스트를 먼저 한다. 명시적 입력은 코드 테스트로 상당 부분 확인 가능하지만, 추상/짧은 입력의 시각적 만족도·복합성·이동 자연스러움은 반드시 사람이 확인한다.

## 11. 절대 피해야 할 오해

- `trapProfile.hazard`와 용암/독지대 terrain을 같은 시스템으로 취급하지 않는다.
- #288의 임시 8방/6방 전제를 현재 생성기 계약으로 취급하지 않는다.
- 프리셋 티어를 플레이어 실제 성장에 맞춰 자동 역산하지 않는다.
- 적이 플레이어 실 성장치에 비례해 즉시 강해지는 구조를 부활시키지 않는다.
- #265 돌진 문제를 원문 정규식 gating 하나로만 해결하려 하지 않는다. 핵심은 구조물 충돌과 연속 이동의 자연스러운 실패 처리다.
- untracked `docs/SEQUENCE_*` 실험 산출물을 사용자가 요청하기 전 삭제하거나 일괄 커밋하지 않는다.
- 실행하지 않은 테스트 결과를 로그나 PR 설명에 적지 않는다.

## 12. 인수인계 직전 확인 명령

```powershell
git branch --show-current
git status --short --branch
git log -8 --oneline --decorate
gh pr list --state open --limit 30
gh issue list --state open --limit 50
npx.cmd tsc --noEmit
npm.cmd run test:mapgen
npm.cmd run test:mapgraph
npm.cmd run test:mapwiring
npm.cmd run build
```

실험 산출물이 많으므로 `git status`에서 보이는 untracked 파일은 정상일 수 있다. 코드/로그 변경이 필요한 경우에만 해당 파일을 선별한다.
