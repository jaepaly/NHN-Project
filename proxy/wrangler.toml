name = "incant-judge-proxy"
main = "worker.js"
compatibility_date = "2026-07-01"

[vars]
# 배포된 GitHub Pages 오리진으로 교체 (개발 중에는 * 허용도 가능)
ALLOWED_ORIGIN = "https://jaepaly.github.io"

# 시크릿 (커밋 금지 — wrangler로 등록):
#   npx wrangler secret put GEMINI_API_KEY
